# Audio Assets

Pump! can import a licensed audio file through the popup and store it locally in extension storage.

Pump! also ships with a packaged default launch sound:

```text
assets/mountains.mp3
```

Imported audio overrides the packaged default until it is cleared in the popup.
